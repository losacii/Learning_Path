### youtube-dl __~ Youtube Downloader
  Install:
    sudo apt-get install youtube-dl

  Usage:
    youtube-dl -F <url>

               -F  : show available formats
               -f  : select format
               -i  : ignore errors
